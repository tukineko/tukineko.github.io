#include "Common.h"

USING_NS_CC;

/*DrawNode* Common::drawGrid(Node* parent)
{
    auto render = RenderTexture::create(winSizeW, winSizeH);
    render->beginWithClear(0, 0, 0, 0);


    for (int x = 0; x < 10; x++) {
        DrawNode* line = DrawNode::create();
        line->drawSegment(Vec2(0, 0), Vec2(0, winSizeH), 2.5f, Color4F::RED);
    }

    render->end();

    parent->addChild(render);
}*/
